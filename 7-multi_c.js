function printXTimesX(x) {
     if (isNaN(x)){
        console.log("Missing number of occurrences");
     } else {
        for (let i = 1; i <= x; i++) {
            console.log("C is fun");
        }
     }
}

printXTimesX('ere')